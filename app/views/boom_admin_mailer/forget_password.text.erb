Hi <%= @dj.name %>，

请点击下面的链接重置密码。

<% if Rails.env.production? %>
  <%= new_password_boombox_dj_boom_admin_url(@dj, host: "music.dan-che.com", token: @dj.confirm_token) %>
<% else %>
  <%= new_password_boombox_dj_boom_admin_url(@dj, host: "boombox.dan-che.com", token: @dj.confirm_token) %>
<% end %>

（如点击后无法打开，请复制链接到浏览器访问）

by 播霸系统邮件
