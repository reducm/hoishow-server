<div class="nav">
  <h1 class="pull-left">新建演出</h1>
</div>

<% flash.each do |type, message| -%>
  <div class="message <%= type.to_s=='alert' ? 'error' : type %>">
    <p><%= message %></p>
  </div>
<% end %>

<% if @concert %>
  <div class="form-group">
    <%= label_tag(:name, "concert名称", class: "col-sm-2 control-label")%>
    <div class="col-sm-10">
      <%= label_tag(:name, @concert.name, class: "form-control-static")%>
    </div>
    <%= render partial: "form", locals: {show: @show, concert: @concert, cities: @concert.cities} %>
  </div>
<%end%>

