<div class="nav">
  <h1 class="pull-left">编辑show</h1>
</div>
<div class="form-group">
  <%= label_tag(:name, "show名称", class: "col-sm-2 control-label")%>
  <div class="col-sm-10">
    <%= label_tag(:name, @show.name, class: "form-control-static")%>
  </div>
</div>


  <%= hidden_field_tag "show_id", @show.id %>

<%= render partial: "form", locals: {show: @show, concert: @concert} %>
<div id="show_area">
  <%= render partial: "area_table", locals: {show: @show} %>
</div>
