<%# encoding: utf-8 %>
<legend><%= link_to "Playlist列表", boombox_dj_playlists_path %>/<%= params[:action] == "new" ? "新建Playlist" : "编辑Playlist"%></legend>
<div class="nav">
  <div class="col-md-offset-4">
    <% if params[:action] == 'new' %>
      <% form_url = "/boombox/dj/playlists" %>
      <% form_method = "POST" %>
      <h1>新建Playlist</h1>
    <% elsif params[:action] == 'edit' %>
      <% form_url = "/boombox/dj/playlists/#{playlist.id}" %>
      <% form_method = "PATCH" %>
      <h1>编辑Playlist</h1>
    <% end %>
  </div>
  <div id="playlist_form">
    <%= simple_form_for( playlist, url: form_url, method: form_method, html: { multipart: :true }) do |f| %>
      <div class="row">
        <div class="col-sm-5">
          <div class="text-center">
            <%= f.input :cover, label: "上传封面(建议大小:500x500)", input_html: {class: "playlist-cover-uploader col-sm-offset-4"} %>
            <div id="playlist_cover_preview">
              <%= boombox_default_avatar playlist.cover_url %>
            </div>
          </div>
        </div>

        <div class="col-sm-6">
          <div class="playlist_info">
            <%= f.input :name, label: "(必填)标题", placeholder: "请输入标题名字" %>
            <%= f.input :is_top, label: "打勾推荐此Playlist" %>

            <%= label_tag "tags", "标签" %>
            <p>
            <% if @tags.any? %>
              <select id="tags" multiple="multiple" class="form-control">
                <% @tags.each do |bt| %>
                  <option value="<%= bt.id %>"><%= bt.name %></option>
                <% end %>
              </select>
            <% else %>
              没有标签
            <% end %>
            </p>
            <%= hidden_field_tag "boom_tag_ids", params[:boom_tag_ids] %>
          </div>
        </div>
      </div>

      <div class="col-md-offset-5">
        <%= submit_tag "保存", class: "btn btn-primary", id: "playlist-submit", data: { disable_with: "loading" } %>
      </div>
    <% end %>
  </div>
</div>

<% if params[:action] == 'edit' %>
  <%= content_tag :div, id: "dj_playlist_tags_already_added", data: {
    data: @tags_already_added_ids
  } do %>
  <% end %>
<% end %>
