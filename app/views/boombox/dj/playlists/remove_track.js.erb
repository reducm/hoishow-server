$('#playlist_tracks').html("<%= j render partial: "/boombox/dj/tracks/list_table", locals: {tracks: @playlist_tracks, list_type: "playlist_tracks"} %>");
$('#playlist_search_track_table').html("<%= j render partial: "/boombox/dj/tracks/list_table", locals: {tracks: @tracks, list_type: "search_tracks"} %>");

// 音乐播放
$(document).ready(function() {
  return $('.track_files').on('click', function() {
    var id, src;
    $('.track_files').removeClass('track_playing');
    $('table.tracks tr td').removeClass('td_playing');
    src = $(this).attr('data-src');
    id = $(this).attr('id');
    $('#track_player').attr('src', src).attr('autoplay', 'autoplay');
    $(this).addClass('track_playing');
    return $('.track_playing').parent().addClass('td_playing');
  });
});
